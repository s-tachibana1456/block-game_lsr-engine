namespace LSR_Engine.src.Common
{
    public enum Actions
    {
        // Movement Actions
        MoveLeft,
        MoveRight,
        MoveDown,
        MoveUp,

        // Rotate Actions
        Rotate,
        RotateReverse,

        // Placement Action
        Drop
    }
}
